Canonical final verification — 2026-09-19

  EditMode: 75 passed / 0 failed   (editmode-TestResults.xml)
  PlayMode: 29 passed / 0 failed   (playmode-TestResults.xml)

One machine artifact per gate. Anything from an earlier run — including a
failed one that was previously mislabelled as an EditMode summary — is under
history/ and is not evidence of the current state.

Coverage added during the contract pass:
  Domain invariants (EditMode)
    - transmission refused from an unrouted relay
    - routing refused until the routing objective's prerequisites hold
    - objective completion refused before its prerequisite objectives
    - the domain completes the transmission objective itself, with no
      outside CompleteObjectiveCommand
  Save schema v3 (EditMode)
    - a pose at the world origin is a recorded pose
    - v2 -> v3 migration records pose validity
    - a position without HasPose is repaired
  Save coherence (EditMode)
    - BroadcastStarted implies route_broadcast
    - BroadcastCompleted implies route_broadcast + broadcast_truth
    - an ending without a completed broadcast is dropped, and kept when it
      completed
    - a missing primary with a valid backup is Recoverable
  Scene lifecycle (PlayMode)
    - a scene that never signals SceneReady fails the transition and does
      not publish SceneChanged
  Input (PlayMode)
    - a reader disabled while UI mode is active leaves no map enabled

Before trusting a count, confirm the assembly rebuilt:
  stat -f %Sm Library/ScriptAssemblies/<name>.dll   (vs the source mtime)
