Standalone player smoke test — final artifact (2026-09-18T21:17:39)

Artifact: BuildEvidence/player-build/EscapeTheElites.app
  result=Succeeded, 8 scenes, 0 errors, 4 warnings, 117,289,247 bytes
  (up from 106,946,789 before the graphics pass — the packed
  metallic-smoothness maps and MAT_Paving are now in the build)

STATIC (verified):
  - 8 scene levels present (level0..level7)
  - all five game assemblies linked: Escape.Core, Escape.Data, Escape.AI,
    Escape.Gameplay, Escape.UI
  - 31.9 MB of packed asset data

DYNAMIC (verified):
    "<app>/Contents/MacOS/My project (7)" -batchmode -nographics \
        -logFile /tmp/player-smoke2.log
  for 30s, then terminated.
  Mono domain reloaded, Null Device renderer (expected with -nographics),
  Input System initialised and shut down cleanly on kill.
  0 exceptions, 0 NullReferenceException, 0 script errors.

CI PATH (partially verified):
  ci/build-player.sh refuses to run while the editor holds Temp/UnityLockfile
  (exit 2, clear message) — confirmed. The build itself was invoked through
  Escape.EditorTools.PlayerBuild.Build, the same entry point the script calls,
  so only the batchmode wrapper is unexercised. A true batchmode run needs the
  editor closed.

NOT VERIFIED — do not read this as control qualification:
  - No input was driven into the player. Mouse/stick look, movement, UI
    open/close and the progression chain were NOT exercised in the built
    player; a windowless player accepts no synthetic input.
  - Controls are covered by PlayerLookTests and PlayerMovementTests against
    the real Input System in the editor, not by this smoke test.
  - Nothing here verifies that the game LOOKS better. That is a visual
    judgement and needs a human at the window.
